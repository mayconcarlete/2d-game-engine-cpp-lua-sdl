#pragma once

class MovementSystem : public {
    public:
        MovementSystem(){
            // Todo RequireComponent<TransformComponent>();
            // + require components

            void Update(){
                // Todo update entity position based on its velocity every frame of the loop
                
            }
        }
}